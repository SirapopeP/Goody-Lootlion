import { Component } from '@angular/core';

@Component({
  selector: 'app-missions-placeholder',
  standalone: true,
  template: `<p>โมดูลภารกิจ — สร้าง / ส่ง / อนุมัติ ตาม <code>/api/missions</code></p>`
})
export class MissionsPlaceholderComponent {}
