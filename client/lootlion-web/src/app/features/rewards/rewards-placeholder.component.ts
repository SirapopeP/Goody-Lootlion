import { Component } from '@angular/core';

@Component({
  selector: 'app-rewards-placeholder',
  standalone: true,
  template: `<p>โมดูลรางวัล — catalog และแลก coin ตาม <code>/api/rewards</code></p>`
})
export class RewardsPlaceholderComponent {}
